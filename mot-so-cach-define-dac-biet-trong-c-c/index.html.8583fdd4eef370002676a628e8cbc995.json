{"head":{"id":6731,"layout":"Post","route":"mot-so-cach-define-dac-biet-trong-c-c","title":"Một số cách “#define” đặc biệt trong C/C++","tags":["tutorial","c++","c"],"date":"2015-04-30T16:23:43.111Z","views":1922,"likes":11,"description":"Đây là từ khóa tiền xử lý, dùng để định nghĩa tên một hằng hoặc kiểu gì đó…. Đối với mình thì nó giúp code \"nhanh gọn\" hơn    nhưng những…","author":{"name":"Huy Nguyen","avatar":"/user_avatar/daynhauhoc.com/david15894/{size}/9742_1.png","username":"david15894"}},"body":"\n<p>Đây là từ khóa <strong>tiền xử lý, dùng để định nghĩa tên một hằng hoặc kiểu</strong> gì đó…. Đối với mình thì nó giúp code \"nhanh gọn\" hơn  <img src=\"http://daynhauhoc.com/images/emoji/apple/joy.png?v=0\" title=\":joy:\" class=\"emoji\" alt=\"joy\" width=\"64\" height=\"64\">  nhưng những <strong>beginner</strong> cũng đừng quá lạm dụng nhé!<br>Chắc mình cũng không cần giải thích nhiều về <strong>“#define”</strong> nữa <img src=\"http://daynhauhoc.com/images/emoji/apple/smiley.png?v=0\" title=\":smiley:\" class=\"emoji\" alt=\"smiley\" width=\"64\" height=\"64\"> nên vào thẳng luôn.<br>Có lẽ đã quen kiểu <strong>“#define PI 3.14…..</strong>” hoặc #define một giá trị nào đấy. Và hôm nay nghỉ lễ rảnh rỗi nên viết ra bài này, sắp tới thi cử nữa nên dạo này không có nhiều thời gian. Bài này nhằm giới thiệu một số kiểu #define (define macro) khác mình hay dùng mà có thể bạn chưa gặp bao giờ =))</p>\n\n<pre><code>#define FOR(i,a,b) for (int i = a; i &lt; b; i++)</code></pre>\n\n<p>Đây là một vòng lặp for và bạn có thể sử dụng nó đơn giản về sau bằng cú pháp: <strong>FOR(i,3,5);</strong> (chạy từ 3 -&gt; 5) ….. hoặc <strong>FOR(i, 1, 100);</strong> (chạy từ 1-&gt;100)</p>\n\n<pre><code>#define for(i,n) for(int i=0;i&lt;n; i++)</code></pre>\n\n<p>chạy từ 0 đến bé hơn n. dùng <strong>for(i, 5);</strong> tức sẽ cho chạy từ 0 đến &lt;5. thay vì cứ phải code lại dài dòng, như vậy ngắn hơn phải không?</p>\n\n<pre><code>#define R1 return 1</code></pre>\n\n<p> thậm chí define cho cả giá trị trả về luôn.</p>\n\n<pre><code>#define CL(a,x) memset(x, a, sizeof(x));</code></pre>\n\n<p>nhìn hàm <strong>memset</strong> chắc cũng biết rồi nhỉ. (cần có thư viện string).</p>\n\n<pre><code>#define filein freopen(\"in.txt\",\"r\",stdin)</code></pre>\n\n<p>Có thể dùng cho cả thao tác mở file.(tất nhiên là đóng cũng được).</p>\n\n<pre><code>#define pr(x) printf(\"Case %d: \",x) in một số nguyên x bằng cú pháp pr(x);\n#define sc(n) scanf(\"%d\",&amp;n) nhập một số nguyên n bằng cú pháp sc(n);\n#define C1(x) cin &gt;&gt; x // gõ C1(x); có vẻ nhanh hơn cin &gt;&gt; x;\n#define C2(x, y) cin &gt;&gt; x &gt;&gt; y // tương tự nhưng nhập 2 số\n#define C3(x, y, z) cin &gt;&gt; x &gt;&gt; y &gt;&gt; z // nhập 3 số</code></pre>\n\n<p>nhập xuất thông thường..</p>\n\n<pre><code>#define pb push_back</code></pre>\n\n<p><strong>push_back</strong> cũng ngắn hơn nhiều rồi.(cần có thư viện vector)</p>\n\n<pre><code>#define max(a, b) ((a) &gt; (b) ? (a) : (b))</code></pre>\n\n<p>tìm max 2 số a và b bằng cú pháp <strong>max(a, b);</strong><br>Và còn nhiều nữa……………….Nhưng để tìm max cho bất kỳ kiểu nào (int, float, double..) thì sao?<br>Bạn có nhớ <strong>template</strong> là gì? là một từ khóa của C++ đặc trưng cho việc tổng quát hóa việc xử lý với các kiểu dữ liệu khác nhau.(thôi tự tìm hiểu thêm nha) =))</p>\n\n<pre><code>template &lt;class T&gt; inline T max(T a, T b){ return a &gt; b ? a : b;}</code></pre>\n\n<p>tìm max 2 số a và b thì chỉ cần dùng cú pháp <strong>max(a, b);</strong> là xong<br><code>template&lt;class T&gt;T sq(T a){ return (a*a); }</code> <br>// tính a^2 bằng cú pháp <strong>sq(a);</strong><br><code>template&lt;class T&gt;T gcd(T a,T b){ return (b==0) ? a : gcd(b,a%b); }</code> <br>// ước số chung lớn nhất của 2 số a, b bằng cú pháp <strong>gcd(a, b);</strong><br><code>template&lt;class T&gt;T lcm(T a,T b){ return (a/gcd(a,b))*b; }</code> <br>// ……….nhỏ nhất<br><code>template&lt;class T&gt;T Pow(T n,T p) { T res=n; for(T i=1;i&lt;p; i++){ res *= n; } return res; }</code> <br>// chỉ là một hàm Pow thôi.<br><code>template&lt;class T&gt;bool isPrime(T n){ for(T i=2; i*i&lt;=n; i++){ if(n%i==0) return false; } return true; }</code> <br>// kiểm tra số nguyên tố.<br>…………….. còn rất nhiều nữa và bạn có thể tự nghĩ ra. tham khảo thêm <strong>typedef</strong><br><strong>có thể một số không cần thiết lắm nhưng mục đích của bài này là giới thiệu cho bạn biết những “khả năng” của #define và template có thể làm được mà bạn chưa biết. HẾT</strong></p>\n","rawBody":"\n<p>Đây là từ khóa <strong>tiền xử lý, dùng để định nghĩa tên một hằng hoặc kiểu</strong> gì đó…. Đối với mình thì nó giúp code \"nhanh gọn\" hơn  <img src=\"http://daynhauhoc.com/images/emoji/apple/joy.png?v=0\" title=\":joy:\" class=\"emoji\" alt=\"joy\" width=\"64\" height=\"64\">  nhưng những <strong>beginner</strong> cũng đừng quá lạm dụng nhé!<br>Chắc mình cũng không cần giải thích nhiều về <strong>“#define”</strong> nữa <img src=\"http://daynhauhoc.com/images/emoji/apple/smiley.png?v=0\" title=\":smiley:\" class=\"emoji\" alt=\"smiley\" width=\"64\" height=\"64\"> nên vào thẳng luôn.<br>Có lẽ đã quen kiểu <strong>“#define PI 3.14…..</strong>” hoặc #define một giá trị nào đấy. Và hôm nay nghỉ lễ rảnh rỗi nên viết ra bài này, sắp tới thi cử nữa nên dạo này không có nhiều thời gian. Bài này nhằm giới thiệu một số kiểu #define (define macro) khác mình hay dùng mà có thể bạn chưa gặp bao giờ =))</p>\n\n<pre><code>#define FOR(i,a,b) for (int i = a; i &lt; b; i++)</code></pre>\n\n<p>Đây là một vòng lặp for và bạn có thể sử dụng nó đơn giản về sau bằng cú pháp: <strong>FOR(i,3,5);</strong> (chạy từ 3 -&gt; 5) ….. hoặc <strong>FOR(i, 1, 100);</strong> (chạy từ 1-&gt;100)</p>\n\n<pre><code>#define for(i,n) for(int i=0;i&lt;n; i++)</code></pre>\n\n<p>chạy từ 0 đến bé hơn n. dùng <strong>for(i, 5);</strong> tức sẽ cho chạy từ 0 đến &lt;5. thay vì cứ phải code lại dài dòng, như vậy ngắn hơn phải không?</p>\n\n<pre><code>#define R1 return 1</code></pre>\n\n<p> thậm chí define cho cả giá trị trả về luôn.</p>\n\n<pre><code>#define CL(a,x) memset(x, a, sizeof(x));</code></pre>\n\n<p>nhìn hàm <strong>memset</strong> chắc cũng biết rồi nhỉ. (cần có thư viện string).</p>\n\n<pre><code>#define filein freopen(\"in.txt\",\"r\",stdin)</code></pre>\n\n<p>Có thể dùng cho cả thao tác mở file.(tất nhiên là đóng cũng được).</p>\n\n<pre><code>#define pr(x) printf(\"Case %d: \",x) in một số nguyên x bằng cú pháp pr(x);\n#define sc(n) scanf(\"%d\",&amp;n) nhập một số nguyên n bằng cú pháp sc(n);\n#define C1(x) cin &gt;&gt; x // gõ C1(x); có vẻ nhanh hơn cin &gt;&gt; x;\n#define C2(x, y) cin &gt;&gt; x &gt;&gt; y // tương tự nhưng nhập 2 số\n#define C3(x, y, z) cin &gt;&gt; x &gt;&gt; y &gt;&gt; z // nhập 3 số</code></pre>\n\n<p>nhập xuất thông thường..</p>\n\n<pre><code>#define pb push_back</code></pre>\n\n<p><strong>push_back</strong> cũng ngắn hơn nhiều rồi.(cần có thư viện vector)</p>\n\n<pre><code>#define max(a, b) ((a) &gt; (b) ? (a) : (b))</code></pre>\n\n<p>tìm max 2 số a và b bằng cú pháp <strong>max(a, b);</strong><br>Và còn nhiều nữa……………….Nhưng để tìm max cho bất kỳ kiểu nào (int, float, double..) thì sao?<br>Bạn có nhớ <strong>template</strong> là gì? là một từ khóa của C++ đặc trưng cho việc tổng quát hóa việc xử lý với các kiểu dữ liệu khác nhau.(thôi tự tìm hiểu thêm nha) =))</p>\n\n<pre><code>template &lt;class T&gt; inline T max(T a, T b){ return a &gt; b ? a : b;}</code></pre>\n\n<p>tìm max 2 số a và b thì chỉ cần dùng cú pháp <strong>max(a, b);</strong> là xong<br><code>template&lt;class T&gt;T sq(T a){ return (a*a); }</code> <br>// tính a^2 bằng cú pháp <strong>sq(a);</strong><br><code>template&lt;class T&gt;T gcd(T a,T b){ return (b==0) ? a : gcd(b,a%b); }</code> <br>// ước số chung lớn nhất của 2 số a, b bằng cú pháp <strong>gcd(a, b);</strong><br><code>template&lt;class T&gt;T lcm(T a,T b){ return (a/gcd(a,b))*b; }</code> <br>// ……….nhỏ nhất<br><code>template&lt;class T&gt;T Pow(T n,T p) { T res=n; for(T i=1;i&lt;p; i++){ res *= n; } return res; }</code> <br>// chỉ là một hàm Pow thôi.<br><code>template&lt;class T&gt;bool isPrime(T n){ for(T i=2; i*i&lt;=n; i++){ if(n%i==0) return false; } return true; }</code> <br>// kiểm tra số nguyên tố.<br>…………….. còn rất nhiều nữa và bạn có thể tự nghĩ ra. tham khảo thêm <strong>typedef</strong><br><strong>có thể một số không cần thiết lắm nhưng mục đích của bài này là giới thiệu cho bạn biết những “khả năng” của #define và template có thể làm được mà bạn chưa biết. HẾT</strong></p>\n","raw":"---json\n{\"id\":6731,\"layout\":\"Post\",\"route\":\"mot-so-cach-define-dac-biet-trong-c-c\",\"title\":\"Một số cách “#define” đặc biệt trong C/C++\",\"tags\":[\"tutorial\",\"c++\",\"c\"],\"date\":\"2015-04-30T16:23:43.111Z\",\"views\":1922,\"likes\":11,\"description\":\"Đây là từ khóa tiền xử lý, dùng để định nghĩa tên một hằng hoặc kiểu gì đó…. Đối với mình thì nó giúp code \\\"nhanh gọn\\\" hơn    nhưng những…\",\"author\":{\"name\":\"Huy Nguyen\",\"avatar\":\"/user_avatar/daynhauhoc.com/david15894/{size}/9742_1.png\",\"username\":\"david15894\"}}\n---\n\n<p>Đây là từ khóa <strong>tiền xử lý, dùng để định nghĩa tên một hằng hoặc kiểu</strong> gì đó…. Đối với mình thì nó giúp code \"nhanh gọn\" hơn  <img src=\"http://daynhauhoc.com/images/emoji/apple/joy.png?v=0\" title=\":joy:\" class=\"emoji\" alt=\"joy\" width=\"64\" height=\"64\">  nhưng những <strong>beginner</strong> cũng đừng quá lạm dụng nhé!<br>Chắc mình cũng không cần giải thích nhiều về <strong>“#define”</strong> nữa <img src=\"http://daynhauhoc.com/images/emoji/apple/smiley.png?v=0\" title=\":smiley:\" class=\"emoji\" alt=\"smiley\" width=\"64\" height=\"64\"> nên vào thẳng luôn.<br>Có lẽ đã quen kiểu <strong>“#define PI 3.14…..</strong>” hoặc #define một giá trị nào đấy. Và hôm nay nghỉ lễ rảnh rỗi nên viết ra bài này, sắp tới thi cử nữa nên dạo này không có nhiều thời gian. Bài này nhằm giới thiệu một số kiểu #define (define macro) khác mình hay dùng mà có thể bạn chưa gặp bao giờ =))</p>\n\n<pre><code>#define FOR(i,a,b) for (int i = a; i &lt; b; i++)</code></pre>\n\n<p>Đây là một vòng lặp for và bạn có thể sử dụng nó đơn giản về sau bằng cú pháp: <strong>FOR(i,3,5);</strong> (chạy từ 3 -&gt; 5) ….. hoặc <strong>FOR(i, 1, 100);</strong> (chạy từ 1-&gt;100)</p>\n\n<pre><code>#define for(i,n) for(int i=0;i&lt;n; i++)</code></pre>\n\n<p>chạy từ 0 đến bé hơn n. dùng <strong>for(i, 5);</strong> tức sẽ cho chạy từ 0 đến &lt;5. thay vì cứ phải code lại dài dòng, như vậy ngắn hơn phải không?</p>\n\n<pre><code>#define R1 return 1</code></pre>\n\n<p> thậm chí define cho cả giá trị trả về luôn.</p>\n\n<pre><code>#define CL(a,x) memset(x, a, sizeof(x));</code></pre>\n\n<p>nhìn hàm <strong>memset</strong> chắc cũng biết rồi nhỉ. (cần có thư viện string).</p>\n\n<pre><code>#define filein freopen(\"in.txt\",\"r\",stdin)</code></pre>\n\n<p>Có thể dùng cho cả thao tác mở file.(tất nhiên là đóng cũng được).</p>\n\n<pre><code>#define pr(x) printf(\"Case %d: \",x) in một số nguyên x bằng cú pháp pr(x);\n#define sc(n) scanf(\"%d\",&amp;n) nhập một số nguyên n bằng cú pháp sc(n);\n#define C1(x) cin &gt;&gt; x // gõ C1(x); có vẻ nhanh hơn cin &gt;&gt; x;\n#define C2(x, y) cin &gt;&gt; x &gt;&gt; y // tương tự nhưng nhập 2 số\n#define C3(x, y, z) cin &gt;&gt; x &gt;&gt; y &gt;&gt; z // nhập 3 số</code></pre>\n\n<p>nhập xuất thông thường..</p>\n\n<pre><code>#define pb push_back</code></pre>\n\n<p><strong>push_back</strong> cũng ngắn hơn nhiều rồi.(cần có thư viện vector)</p>\n\n<pre><code>#define max(a, b) ((a) &gt; (b) ? (a) : (b))</code></pre>\n\n<p>tìm max 2 số a và b bằng cú pháp <strong>max(a, b);</strong><br>Và còn nhiều nữa……………….Nhưng để tìm max cho bất kỳ kiểu nào (int, float, double..) thì sao?<br>Bạn có nhớ <strong>template</strong> là gì? là một từ khóa của C++ đặc trưng cho việc tổng quát hóa việc xử lý với các kiểu dữ liệu khác nhau.(thôi tự tìm hiểu thêm nha) =))</p>\n\n<pre><code>template &lt;class T&gt; inline T max(T a, T b){ return a &gt; b ? a : b;}</code></pre>\n\n<p>tìm max 2 số a và b thì chỉ cần dùng cú pháp <strong>max(a, b);</strong> là xong<br><code>template&lt;class T&gt;T sq(T a){ return (a*a); }</code> <br>// tính a^2 bằng cú pháp <strong>sq(a);</strong><br><code>template&lt;class T&gt;T gcd(T a,T b){ return (b==0) ? a : gcd(b,a%b); }</code> <br>// ước số chung lớn nhất của 2 số a, b bằng cú pháp <strong>gcd(a, b);</strong><br><code>template&lt;class T&gt;T lcm(T a,T b){ return (a/gcd(a,b))*b; }</code> <br>// ……….nhỏ nhất<br><code>template&lt;class T&gt;T Pow(T n,T p) { T res=n; for(T i=1;i&lt;p; i++){ res *= n; } return res; }</code> <br>// chỉ là một hàm Pow thôi.<br><code>template&lt;class T&gt;bool isPrime(T n){ for(T i=2; i*i&lt;=n; i++){ if(n%i==0) return false; } return true; }</code> <br>// kiểm tra số nguyên tố.<br>…………….. còn rất nhiều nữa và bạn có thể tự nghĩ ra. tham khảo thêm <strong>typedef</strong><br><strong>có thể một số không cần thiết lắm nhưng mục đích của bài này là giới thiệu cho bạn biết những “khả năng” của #define và template có thể làm được mà bạn chưa biết. HẾT</strong></p>\n","__filename":"posts/6731.md","__url":"/mot-so-cach-define-dac-biet-trong-c-c/","__resourceUrl":"/mot-so-cach-define-dac-biet-trong-c-c/index.html","__dataUrl":"/mot-so-cach-define-dac-biet-trong-c-c/index.html.8583fdd4eef370002676a628e8cbc995.json"}